import Login from "@/app/login"
import { LogBox } from 'react-native';

LogBox.ignoreAllLogs(); // Disables all warning notifications in the app


const Index = () => {
  return <Login></Login>
};

export default Index